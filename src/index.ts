/**
 * x.object({
 *   name: x.string(),
 *   age: x.number(),
 *   email: x.string(),
 *   bioUrl: x.string(),
 *   tags: x.array(x.string()),
 *   any: x.any(),
 *   transform: x.object({
 *     name: x.string()
 *   })
 * })
 */

export class x {
}
